<% if logged? %>
    <%= current_user.friendships.count %><br>
    <% @movies.each do |movie| %>
        <%= render 'movie', :movie => movie %>
    <% end %>

    <%= will_paginate @movies %>

<% end %>